//Using different ways to write string to the user
#include <stdio.h>

int main()
{
    char city[100];
    printf("Enter a city name: ");
    scanf("%s", city);
    printf("%s", city);

    return 0;
}