//Storage class: auto
#include <stdio.h>

int main()
{
    int a;
    float b;
    char c;
    printf("%d %f %c", a, b, c);/* There is no syntax error but values of data type is not
                                       assigned. Hence the computer will show us garbage values 
                                       during the run time */

    return 0;
}