// Boolean array
#include <stdio.h>
#include <stdbool.h>

int main()
{
    bool x[2] = {true, false};
    for (int i = 0; i < 2; i++)
    {
        printf("%d ", x[i]);
    }

    return 0;
}